/** @jsxImportSource @emotion/react */

const LoginLogo = ({image}) => {
  return (
    <img
      css={{
        width: '20%',
        height: 'auto',
      }}
      src={image}
      alt="appLogo" />
  );
}

export default LoginLogo;
